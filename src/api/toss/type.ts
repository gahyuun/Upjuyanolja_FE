export type PointChargeType = {
  orderId: string;
  tradeAt: string;
  orderName: string;
  amount: number;
};
export type TossRequestType = {
  paymentKey: string;
  orderId: string;
  amount: number;
};
